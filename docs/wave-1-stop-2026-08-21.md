# Wave 1 — STOPPED before push. Three decisions needed.

**`main` was not touched. Nothing was pushed. Nothing was deployed.** The wave is built, verified
and waiting on two local branches. Everything below was measured, not estimated.

**Baselines:** `origin/main` = `f04c01b` · `origin/cloudflare-staging` = `622b9c0` · backlog = **199**
by patch-id (386 by `rev-list`, 331 examined, 132 already on `main` under different SHAs).

**Ready to push:**

| branch | commits | end state |
|---|---|---|
| `promote/wave1-infra` (`1bba842`) | 76 | 45 files: `scripts/` 34, `ops/` 5, `docs/` 3, `CONTRIBUTING.md`, `.gitignore`, `.github/workflows/probe-302-305-production.yml` |
| `promote/wave1-infra-noworkflow` (`1811b0e`) | 76 + 1 removal | the same, **minus** the probe workflow — zero `.github/` change on `main` |

---

## Why it stopped

### STOP 1 — your own step 4 condition fired: the backlog dropped by 74, not 76

Measured against the built branch before pushing:

```
backlog before (vs origin/main)          199
backlog after  (vs promote/wave1-infra)  125
drop                                      74        promoted 76
```

**The selection method is not wrong. The count check is.** Both residuals are explained, and both
are shapes that will recur in every later wave:

- **`b915483b`** *("ci: make main-ahead-of-staging fail the build")* applied **partially**. One of
  its three files, `.github/workflows/staging.yml`, is already on `main` — so the cherry-pick's
  diff, and therefore its patch-id, differs from the original. Content promoted; patch-id does not
  match. Verified: original 3 files/293 insertions, applied 2 files/272 insertions.
- **`74b0529e`** *("probe: the tax-rate refusal, live against the deployed worker")* applied
  **empty**. Its only file, `scripts/probe-menu-item-requires-tax-rate-staging.ts`, is already on
  `main` byte-identical (blob `cb928bf1` on both branches), having arrived inside the squashed
  promotion `5c9d31d`. There was never anything to promote.

The check that *does* hold, and that I would substitute: **nothing left the backlog that was not in
the wave** — verified empty — plus an individual explanation for every promoted commit still
reading `+`. Written up as step 6 of `docs/promotion-runbook.md`.

**Decision needed:** accept the set-based check in place of the count check for waves 2–6, or treat
74 ≠ 76 as a genuine failure and have me re-cut the wave.

### STOP 2 — "the 40" does not exist as a reproducible set. By diff there are 77.

The inventory groups by **commit subject**, first-match, and says so in its own closing section.
Subject is not diff, and the classifier was not recorded, so the specific 40 cannot be recovered.

Classified by the files they actually touch, **77 of the 199 backlog commits touch no runtime file
at all**. They are not extra work smuggled in — they are the same commits, filed elsewhere by the
subject classifier:

| where the inventory filed them | count | what they are |
|---|---|---|
| "infra/CI, docs, probes" | ~40 | the wave as briefed |
| "security-labelled" | 18 | empty CI marker commits — their subjects say *"re-verify the production redaction…"*, so the security keyword matched first. The inventory already calls these markers and excludes them from wave 4 ("~10 real, **minus markers**"). |
| "other" | ~20 | probe scripts, sim fixes, drift-baseline commits |

Waves 2–6 account for 121 commits (9 + 8 + ~10 + 80 + 14). 199 − 121 = 78 ≈ the 77. **These
commits belong to no other wave**, so promoting all 77 takes nothing from waves 2–6 — but it is
roughly double what you briefed, and that is your call, not mine.

**Decision needed:** wave 1 is the 77-by-diff (76 after the drop below), or you want a narrower cut.
If narrower, I need the rule — any line between "infra/CI/docs/probe" and "other" is arbitrary and
I would be inventing it.

**Already dropped, and this one is not a judgement call:** `d3eba569`
*("test(order-editing): make the staging probe prove BOTH directions")* modifies
`scripts/probe-order-edit-lock-race-staging.ts`, which is **created by `ae9c65e9`** — a wave 3
order-editing runtime commit. It is a wave 3 commit wearing a `scripts/` diff, and it is the only
commit in the 77 that conflicts. It goes with wave 3. 77 → 76.

### STOP 3 — the probe workflow's own header rules it off `main`

21 of the 76 commits touch `.github/workflows/probe-302-305-production.yml`, so it lands on `main`
as a side effect of promoting the probe scripts it drives. Its header records the ruling:

> TRIGGERED BY A COMMIT MARKER, not workflow_dispatch. A dispatchable workflow must exist on the
> DEFAULT branch, and **this instrument has no business on main** — it would be repo state that
> production carries for a check production never runs.

It is inert on `main` either way: `on: push: branches: [cloudflare-staging]`, no
`workflow_dispatch`, so a push to `main` can never fire it. But it also carries a step that is not
read-only — *"Verify org merge independently (**one self-assignment write**)"* — and I am not going
to put a file on the default branch that its own author ruled off it.

`promote/wave1-infra-noworkflow` is that branch with the file removed in a final commit; its
cumulative diff against `main` contains **no `.github/` change at all**. Same 76 promoted commits,
plus one removal.

**Decision needed:** `promote/wave1-infra` (workflow lands, inert by trigger, contradicts the
ruling) or `promote/wave1-infra-noworkflow` (ruling honoured). I recommend the latter.

---

## What I verified, and it all passed

- **No runtime file, anywhere in the cumulative diff.** `app/ lib/ components/ hooks/ contexts/
  types/ workers/ payments/ public/ supabase/ __tests__/ tests/ middleware.ts next.config.mjs
  open-next.config.ts wrangler*.toml package*.json tsconfig.json vercel.json firestore.rules
  storage.rules firebase.json` — grep of the end-state diff returns nothing.
- **No migration.** Zero `supabase/` paths in all 199-commit classification for this set.
- **No workflow that affects production.** None of the 76 touches any of the six scripts
  `production-worker.yml` runs as its deploy gate. And **nothing on `main` triggers on a push to
  `main`** — every production workflow is `workflow_dispatch` only — so pushing this wave is not a
  deploy, and step 7 would be a separate deliberate act.
- **`apply-org-merge.yml` nets out.** Added by `f31f68bb`, removed by `907f897e` ("unwired by
  ruling"). It is in two commits and in no end state. Confirmed via `diff --name-status`.
- **Nothing runtime imports `scripts/`.** grep across `app lib components hooks contexts types
  workers payments middleware.ts` on staging: empty.
- **All 76 cherry-pick cleanly**, oldest first, zero conflicts, once `d3eba569` is dropped.
- **No collateral.** Nothing left the backlog that was not in the wave.

## One thing to flag, not a blocker

`main`'s `.github/workflows/staging.yml` already invokes `node scripts/check-branch-drift.mjs`
(line 584), but **`scripts/check-branch-drift.mjs` is absent from `main`**. It is a dangling
reference today — dormant, because `staging.yml` only fires on pushes to `cloudflare-staging` where
the script does exist. Wave 1 happens to repair it by bringing the script across.

## Two things landing on `main` that are worth a second look

- **`ops/org-merge/*.sql` (5 files)** — production `UPDATE`/rename statements for the ChowNow →
  Riviera organisation merge, plus the before-snapshot. Inert as files, and the workflow that ran
  them is removed in the same wave, so nothing on `main` can execute them. Still: production-
  mutating SQL on the default branch. Say if you would rather it stayed on staging.
- **16 `*-production-readonly.ts` probe scripts and
  `verify-deploy1-redaction-live-production.ts`** — they need the production service-role key,
  which exists only as a GitHub secret, and on `main` nothing invokes them.

## To resume in the morning

```bash
cd C:/Users/223125318/Desktop/mvp/restaurant-menu-screen
git push origin promote/wave1-infra-noworkflow:main      # or promote/wave1-infra:main
```

No deploy follows automatically. `/api/version` on the three hostnames would then need
`production-worker.yml` dispatched manually, and 20/20 identical samples per hostname — the worker
rolls out gradually and one request can read either version for about two minutes.

---

## The 76 commits, oldest first

SHAs are on `cloudflare-staging`. `d3eba569` (dropped to wave 3) is not listed.

```
fb9583c 2026-08-11 test(#125): non-mocked staging probe for the order_requests -> Accept money seam
a230d67 2026-08-11 test(#125): extend the accept-seam probe to hosted, kiosk and concurrent Accept
90555f1 2026-08-11 test(#125): cover size/add-on modifier pricing through the accept seam
940d1c9 2026-08-15 test(qr): two-sided staging probe for the four QR customer-flow exposures
69aeea9 2026-08-16 test(qr): simulate redesign events A-Q against the deployed staging worker
cdde535 2026-08-16 test(qr): events A-Q against staging — 24 checks, 0 fails, including a real terminal settle
be461c7 2026-08-16 test(qr): exercise the ADDITION path live — 26 checks, 0 fails
59e8167 2026-08-16 test(qr): live coverage for #249 and #248, and the vacuous-pass they invited
b11940f 2026-08-16 fix(sim): survive table debris, and stop printing a clean tally after an abort
925ddaf 2026-08-16 fix: #267 - a worktree teardown that does not eat the shared node_modules
0addfba 2026-08-16 probe: #196 - settle whether the guest API ever returns a non-string order id
b8755fb 2026-08-16 fix(sim): clean up payments and payment_events, which cleanup never touched
fab95b7 2026-08-16 test(sim): B-money - the shared tab's line figures are what the customer pays
feae081 2026-08-17 docs: record the #302 split-promotion hazard, and #305
fe34dc0 2026-08-17 docs: record #305's promotion ORDER constraint
b915483 2026-08-17 ci: make main-ahead-of-staging fail the build instead of going unnoticed
c98ec43 2026-08-17 ci(drift): shrink the baseline by six - #242, #223 and the #266 pin are reconciled
6a7bec0 2026-08-17 docs: the copy sheet - 30 strings to write, grouped by screen
fe3758a 2026-08-17 docs: production migration review - 8 absent, 4 recommended, nothing applied
3368ac1 2026-08-17 ci(drift): baseline the two promotion commits, with the reason traced
19a3864 2026-08-17 fix(drift): repair the bracket my own edit misplaced
4d4b7dc 2026-08-18 fix(#310): a PROMOTED state, so a promotion stops reading as drift
0005e7e 2026-08-18 chore(#310): drop 9fcb147 from KNOWN_ABSENT — it reconciled
17aceca 2026-08-18 ci: run the #302/#305 production redaction probe where the credential exists
e25211a 2026-08-18 ci: fire the redaction probe from a commit marker, not a dispatch
7dd09da 2026-08-18 probe: re-verify the production redaction after cd5e01a [probe-302-305-production]
a6b30f9 2026-08-18 test(sim): many tables at once, forever — the concurrency the single arc cannot see
1e3c656 2026-08-18 probe: re-verify the production redaction after 77dbf76 [probe-302-305-production]
43ff90d 2026-08-18 probe: re-verify the production redaction after ef9820b [probe-302-305-production]
08d1839 2026-08-18 probe: how long has anything been waiting for the restaurant [probe-302-305-production]
87ed64a 2026-08-18 probe: re-verify the production redaction after 5a0cb9a [probe-302-305-production]
48f9b86 2026-08-18 probe: re-verify the production redaction after 4e876e4 [probe-302-305-production]
5dae39c 2026-08-18 probe: measure how long an answer normally takes, to ground a threshold [probe-302-305-production]
ecc3996 2026-08-18 probe: measure accept time via the request->order link [probe-302-305-production]
26927f8 2026-08-18 probe: which session-boundary fix the schema permits [probe-302-305-production]
6c05b81 2026-08-18 probe: re-verify the production redaction after e13340c [probe-302-305-production]
5ffe8b3 2026-08-18 probe: re-verify the production redaction after 68c1eb9 [probe-302-305-production]
0d5167d 2026-08-18 probe: the VAT split on production order #10 [probe-302-305-production]
433f098 2026-08-18 probe: fix the tax-rate read that silently returned nothing [probe-302-305-production]
dce45ab 2026-08-18 probe: count menu items with no tax rate set [probe-302-305-production]
74b0529 2026-08-18 probe: the tax-rate refusal, live against the deployed worker
bfc9271 2026-08-18 probe: re-verify Deploy 1 redaction after the 5c9d31d production deploy [probe-302-305-production]
507cac0 2026-08-18 probe: re-verify Deploy 1 redaction after the 632d00d production deploy [probe-302-305-production]
60b29d3 2026-08-18 probe: measure the sound-alert context on production, read-only [probe-302-305-production]
880cb14 2026-08-19 probe: re-verify Deploy 1 redaction after a274d9e [probe-302-305-production]
9fd8d14 2026-08-19 probe: measure cash payment_method on production tab orders, read-only [probe-302-305-production]
8e3e8b2 2026-08-19 probe: measure exactly what close_table_session touches, and what survives it
a27ed05 2026-08-19 probe: count abandoned-candidate requests and the tab-PIN census, read-only [probe-302-305-production]
f8e7afe 2026-08-19 probe: the End Session hole, measured end to end in both PIN states
913740e 2026-08-19 ci: baseline four drift entries whose behaviour is verified present on staging
2eef0c2 2026-08-19 probe: what state the 08:47 Mingle decline left its order in [probe-302-305-production]
f735ffb 2026-08-19 probe: re-run the Mingle decline check with working queries [probe-302-305-production]
06363e0 2026-08-19 fix(probe): the Mingle decline probe searched nothing and reported an absence
5f104d4 2026-08-19 probe: re-run the Mingle decline check with working queries [probe-302-305-production]
42c73d7 2026-08-19 probe: re-verify Deploy 1 redaction after wave 1 (auth 77f4c2b) [probe-302-305-production]
e51db29 2026-08-19 probe: re-verify Deploy 1 redaction after wave 2 (payments ef5444f) [probe-302-305-production]
17085e1 2026-08-19 probe: re-verify Deploy 1 redaction after wave 3 (5b8c95b) [probe-302-305-production]
934f253 2026-08-19 probe: FNB ChowNow staff roster and rename history, read-only [probe-302-305-production]
80e90c0 2026-08-19 probe: is the organisations layer live on production, read-only [probe-302-305-production]
b2a98ea 2026-08-19 probe: has the organisations feature ever been used on production [probe-302-305-production]
1af8087 2026-08-19 probe: what a restaurant moving between organisations would land on [probe-302-305-production]
7271ea8 2026-08-19 verify: Deploy 1's redaction, live production, with a positive control [probe-302-305-production]
03c0033 2026-08-19 fix: the redaction verifier's own positive control was void
22293b0 2026-08-19 chore: re-run the redaction verifier after its two fixes [probe-302-305-production]
1d0ea0d 2026-08-19 fix: pick a tab the view route will actually serve [probe-302-305-production]
4124a53 2026-08-19 fix: syntax error from collapsed escapes in the NOT RUNNABLE message [probe-302-305-production]
bd51b53 2026-08-19 probe: pre-merge snapshot and catalogue collision check [probe-302-305-production]
052a9b8 2026-08-19 ops: the org merge statements and the pre-merge snapshot, unexecuted
d5e6c4b 2026-08-19 ops: fill the merge values, split the coke rename out, probe dsgdsg [probe-302-305-production]
f31f68b 2026-08-19 ops: the merge runner, the verification, and a check on whether SQL can run at all [probe-302-305-production]
907f897 2026-08-19 ops: remove the automated production-SQL runner, unwired by ruling
e72bf1c 2026-08-19 probe: why the Locations list shows only Riviera after the merge [probe-302-305-production]
1b3d07c 2026-08-19 chore: post-deploy checks for 7191c55 [probe-302-305-production]
0f50d6a 2026-08-19 probe: wire the independent merge verification back in [probe-302-305-production]
b01bda8 2026-08-19 probe: what Add Location created and what the new site lacks [probe-302-305-production]
d913a34 2026-08-19 chore: post-deploy redaction check for 26acbda [probe-302-305-production]
```
