# ADR-004: Order Service Architecture

**Status:** Absorbed by ADR-005
**Date:** 2026-08-27

No decision was ever recorded under this number.

This file was created as a reserved slot when the ADR folder was
established, alongside 002 and 003. It carried a title and
`Status: Proposed — content pending`, and nothing else. There is no
context, no decision and no set of assumptions here to replace, which is
why this is not marked `Superseded by`.

ADR-005 covers this ground: how an order reaches a station, line state,
waiter-to-table assignment, and tabs created without a customer session.
Rather than have 005 cross-reference an empty file, 005 takes this
number's scope.

The number is retained deliberately. Deleting it would open a hole in the
sequence while ADR-006 is being renumbered in a parallel session, and a
hole plus a renumber is how a third collision happens.

See `005-waiter-led-service.md`.
